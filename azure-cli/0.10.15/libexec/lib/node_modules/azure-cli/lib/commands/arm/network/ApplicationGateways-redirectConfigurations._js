/**
 * Copyright (c) Microsoft.  All rights reserved.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// Warning: This code was generated by a tool.
// 
// Changes to this file may cause incorrect behavior and will be lost if the
// code is regenerated.

'use strict';

var generatorUtils = require('../../../util/generatorUtils');
var resourceUtils = require('../resource/resourceUtils');
var util = require('util');

var profile = require('../../../util/profile');
var utils = require('../../../util/utils');

var $ = utils.getLocaleString;

exports.init = function (cli) {
  var network = cli.category('network')
    .description($('Commands to manage network resources'));
  var applicationGateways = network.category('application-gateway')
    .description($('Commands to manage application gateways'));
  var redirectConfigurations = applicationGateways.category('redirect-config')
    .description($('Commands to manage redirect configurations'));

  redirectConfigurations.command('create [resource-group] [gateway-name] [name]')
    .description($('Create a redirect configurations'))
    .usage('[options] <resource-group> <gateway-name> <name>')
    .option('-g, --resource-group <resource-group>', $('the name of the resource group'))
    .option('-w, --gateway-name <gateway-name>', $('the gateway name'))
    .option('-n, --name <name>', $('the name of the redirect configuration'))
    .option('-r, --redirect-type [redirect-type]', $('supported http redirection types -' +
      '\n     Permanent, Temporary, Found, SeeOther'))
    .option('-d, --target-listener-id [target-listener-id]', $('sets target listener.'))
    .option('-p, --include-path [include-path]', $('include path in the redirected url'))
    .option('-u, --target-url [target-url]', $('url to redirect the request to'))
    .option('-i, --include-query-string [include-query-string]', $('include query string in the redirected' +
      '\n     url'))
    .option('-s, --subscription <subscription>', $('the subscription identifier'))
    .execute(function(resourceGroup, gatewayName, name, options, _) {
      resourceGroup = cli.interaction.promptIfNotGiven($('resource group : '), resourceGroup, _);
      gatewayName = cli.interaction.promptIfNotGiven($('gateway name : '), gatewayName, _);
      name = cli.interaction.promptIfNotGiven($('name : '), name, _);

      var subscription = profile.current.getSubscription(options.subscription);
      var networkManagementClient = utils.createNetworkManagementClient(subscription);

      var index = 0;
      var applicationGateway;
      var progress = cli.interaction.progress(util.format($('Looking up the application gateway "%s"'), gatewayName));
      try {
        applicationGateway = networkManagementClient.applicationGateways.get(resourceGroup, gatewayName, null, _);
      } catch (e) {
        if (e.statusCode === 404) {
          applicationGateway = null;
        } else {
          throw e;
        }
      } finally {
        progress.end();
      }

      if (!applicationGateway) {
        throw new Error(util.format($('application gateway with name "%s" not found in the resource group "%s"'), gatewayName, resourceGroup));
      }

      if(utils.findFirstCaseIgnore(applicationGateway.redirectConfigurations, {name: name})) {
        throw new Error(util.format($('redirect configurations with name "%s" already exists in the "%s"'), name, gatewayName));
      }

      var parameters = {};
      if(!parameters.redirectConfigurations) {
        parameters.redirectConfigurations = [];
      }
      if(!parameters.redirectConfigurations[index]) {
        parameters.redirectConfigurations[index] = {};
      }
      if(options.redirectType) {
        parameters.redirectConfigurations[index].redirectType = options.redirectType;
      }

      if(!parameters.redirectConfigurations[index].targetListener) {
        parameters.redirectConfigurations[index].targetListener = {};
      }
      if(options.targetListenerId) {
        parameters.redirectConfigurations[index].targetListener.id = options.targetListenerId;
      }

      if(options.includePath) {
        parameters.redirectConfigurations[index].includePath = utils.parseBool(options.includePath);
      }

      if(options.targetUrl) {
        parameters.redirectConfigurations[index].targetUrl = options.targetUrl;
      }
      if(options.targetUrl !== undefined) {
        delete parameters.redirectConfigurations[index].includePath;
      }

      if(options.includeQueryString) {
        parameters.redirectConfigurations[index].includeQueryString = utils.parseBool(options.includeQueryString);
      }

      parameters.redirectConfigurations[index].name = name;
      applicationGateway.redirectConfigurations.push(parameters.redirectConfigurations[index]);

      generatorUtils.removeEmptyObjects(parameters);
      progress = cli.interaction.progress(util.format($('Creating redirect configurations in "%s"'), gatewayName));
      try {
        applicationGateway = networkManagementClient.applicationGateways.createOrUpdate(resourceGroup, gatewayName, applicationGateway, _);
      } finally {
        progress.end();
      }

      cli.interaction.formatOutput(applicationGateway, generatorUtils.traverse);
    });

  redirectConfigurations.command('set [resource-group] [gateway-name] [name]')
    .description($('Update a redirect configurations'))
    .usage('[options] <resource-group> <gateway-name> <name>')
    .option('-g, --resource-group <resource-group>', $('the name of the resource group'))
    .option('-w, --gateway-name <gateway-name>', $('the gateway name'))
    .option('-n, --name <name>', $('the name of the redirect configuration'))
    .option('-r, --redirect-type [redirect-type]', $('supported http redirection types -' +
      '\n     Permanent, Temporary, Found, SeeOther'))
    .option('-d, --target-listener-id [target-listener-id]', $('sets target listener.'))
    .option('-p, --include-path [include-path]', $('include path in the redirected url'))
    .option('-u, --target-url [target-url]', $('url to redirect the request to'))
    .option('-i, --include-query-string [include-query-string]', $('include query string in the redirected' +
      '\n     url'))
    .option('-s, --subscription <subscription>', $('the subscription identifier'))
    .execute(function(resourceGroup, gatewayName, name, options, _) {
      resourceGroup = cli.interaction.promptIfNotGiven($('resource group : '), resourceGroup, _);
      gatewayName = cli.interaction.promptIfNotGiven($('gateway name : '), gatewayName, _);
      name = cli.interaction.promptIfNotGiven($('name : '), name, _);

      var subscription = profile.current.getSubscription(options.subscription);
      var networkManagementClient = utils.createNetworkManagementClient(subscription);

      var applicationGateway;
      var progress = cli.interaction.progress(util.format($('Looking up the application gateway "%s"'), gatewayName));
      try {
        applicationGateway = networkManagementClient.applicationGateways.get(resourceGroup, gatewayName, null, _);
      } catch (e) {
        if (e.statusCode === 404) {
          applicationGateway = null;
        } else {
          throw e;
        }
      } finally {
        progress.end();
      }

      if (!applicationGateway) {
        throw new Error(util.format($('application gateway with name "%s" not found in the resource group "%s"'), gatewayName, resourceGroup));
      }

      var redirectConfiguration = utils.findFirstCaseIgnore(applicationGateway.redirectConfigurations, {name: name});
      var index = utils.indexOfCaseIgnore(applicationGateway.redirectConfigurations, {name: name});
      if(!redirectConfiguration) {
        throw new Error(util.format($('redirect configurations with name "%s" not found in the "%s"'), name, gatewayName));
      }

      var parameters = applicationGateway;
      if(!parameters.redirectConfigurations) {
        parameters.redirectConfigurations = [];
      }
      if(!parameters.redirectConfigurations[index]) {
        parameters.redirectConfigurations[index] = {};
      }
      if(options.redirectType) {
        parameters.redirectConfigurations[index].redirectType = options.redirectType;
      }

      if(!parameters.redirectConfigurations[index].targetListener) {
        parameters.redirectConfigurations[index].targetListener = {};
      }
      if(options.targetListenerId) {
        if(!utils.argHasValue(options.targetListenerId)) {
          delete parameters.redirectConfigurations[index].targetListener.id;
        } else {
          parameters.redirectConfigurations[index].targetListener.id = options.targetListenerId;
        }
      }

      if(options.includePath) {
        parameters.redirectConfigurations[index].includePath = utils.parseBool(options.includePath);
      }

      if(options.targetUrl) {
        if(!utils.argHasValue(options.targetUrl)) {
          delete parameters.redirectConfigurations[index].targetUrl;
        } else {
          parameters.redirectConfigurations[index].targetUrl = options.targetUrl;
        }
      }
      if(options.targetUrl !== undefined) {
        delete parameters.redirectConfigurations[index].includePath;
      }

      if(options.includeQueryString) {
        parameters.redirectConfigurations[index].includeQueryString = utils.parseBool(options.includeQueryString);
      }

      generatorUtils.removeEmptyObjects(parameters);
      progress = cli.interaction.progress(util.format($('Updating redirect configurations in "%s"'), gatewayName));
      try {
        applicationGateway = networkManagementClient.applicationGateways.createOrUpdate(resourceGroup, gatewayName, applicationGateway, _);
      } finally {
        progress.end();
      }
      cli.interaction.formatOutput(applicationGateway, generatorUtils.traverse);
    });

  redirectConfigurations.command('delete [resource-group] [gateway-name] [name]')
    .description($('Delete a redirect configurations'))
    .usage('[options] <resource-group> <gateway-name> <name>')
    .option('-g, --resource-group <resource-group>', $('the name of the resource group'))
    .option('-w, --gateway-name <gateway-name>', $('the gateway name'))
    .option('-n, --name <name>', $('the name of the redirect configuration'))
    .option('-q, --quiet', $('quiet mode, do not ask for delete confirmation'))
    .option('-s, --subscription <subscription>', $('the subscription identifier'))
    .execute(function(resourceGroup, gatewayName, name, options, _) {
      resourceGroup = cli.interaction.promptIfNotGiven($('resource group : '), resourceGroup, _);
      gatewayName = cli.interaction.promptIfNotGiven($('gateway name : '), gatewayName, _);
      name = cli.interaction.promptIfNotGiven($('name : '), name, _);

      var subscription = profile.current.getSubscription(options.subscription);
      var networkManagementClient = utils.createNetworkManagementClient(subscription);

      var applicationGateway;
      var progress = cli.interaction.progress(util.format($('Looking up the application gateway "%s"'), gatewayName));
      try {
        applicationGateway = networkManagementClient.applicationGateways.get(resourceGroup, gatewayName, null, _);
      } catch (e) {
        if (e.statusCode === 404) {
          applicationGateway = null;
        } else {
          throw e;
        }
      } finally {
        progress.end();
      }

      if (!applicationGateway) {
        throw new Error(util.format($('application gateway "%s" not found in the resource group "%s"'), gatewayName, resourceGroup));
      }

      var index = utils.indexOfCaseIgnore(applicationGateway.redirectConfigurations, {name: name});
      if (index === -1) {
        throw new Error(util.format($('redirect configurations "%s" not found in the gatewayName "%s"'), name, applicationGateway.name));
      }

      if (!options.quiet && !cli.interaction.confirm(util.format($('Delete redirect configurations with name "%s" from "%s"? [y/n] '), name, gatewayName), _)) {
        return;
      }

      applicationGateway.redirectConfigurations.splice(index, 1);
      progress = cli.interaction.progress('Deleting redirect configurations');
      try {
        applicationGateway = networkManagementClient.applicationGateways.createOrUpdate(resourceGroup, gatewayName, applicationGateway, _);
      } finally {
        progress.end();
      }

      cli.interaction.formatOutput(applicationGateway, generatorUtils.traverse);
    });

  redirectConfigurations.command('show [resource-group] [gateway-name] [name]')
    .description($('Show a redirect configurations'))
    .usage('[options] <resource-group> <gateway-name> <name>')
    .option('-g, --resource-group <resource-group>', $('the name of the resource group'))
    .option('-w, --gateway-name <gateway-name>', $('the gateway name'))
    .option('-n, --name <name>', $('the name of the redirect configuration'))
    .option('-s, --subscription <subscription>', $('the subscription identifier'))
    .execute(function(resourceGroup, gatewayName, name, options, _) {
      resourceGroup = cli.interaction.promptIfNotGiven($('resource group : '), resourceGroup, _);
      gatewayName = cli.interaction.promptIfNotGiven($('gateway name : '), gatewayName, _);
      name = cli.interaction.promptIfNotGiven($('name : '), name, _);

      var subscription = profile.current.getSubscription(options.subscription);
      var networkManagementClient = utils.createNetworkManagementClient(subscription);

      var applicationGateway;
      var progress = cli.interaction.progress(util.format($('Looking up the application gateway "%s"'), gatewayName));
      try {
        applicationGateway = networkManagementClient.applicationGateways.get(resourceGroup, gatewayName, null, _);
      } catch (e) {
        if (e.statusCode === 404) {
          applicationGateway = null;
        } else {
          throw e;
        }
      } finally {
        progress.end();
      }

      if (!applicationGateway) {
        throw new Error(util.format($('application gateway with name "%s" not found in the resource group "%s"'), gatewayName, resourceGroup));
      }

      var redirectConfiguration = utils.findFirstCaseIgnore(applicationGateway.redirectConfigurations, {name: name});
      if(!redirectConfiguration) {
        cli.output.warn(util.format($('redirect configurations with name "%s" not found in the gatewayName "%s"'), name, applicationGateway.name));
      }

      cli.interaction.formatOutput(redirectConfiguration, generatorUtils.traverse);
    });

  redirectConfigurations.command('list [resource-group] [gateway-name]')
    .description($('List redirect configurations'))
    .usage('[options] <resource-group> <gateway-name>')
    .option('-g, --resource-group <resource-group>', $('the name of the resource group'))
    .option('-w, --gateway-name <gateway-name>', $('the gateway name'))
    .option('-s, --subscription <subscription>', $('the subscription identifier'))
    .execute(function(resourceGroup, gatewayName, options, _) {
      var subscription = profile.current.getSubscription(options.subscription);
      var networkManagementClient = utils.createNetworkManagementClient(subscription);

      var applicationGateway = null;
      var progress = cli.interaction.progress(util.format($('Looking up the application gateway "%s"'), gatewayName));
      try {
        applicationGateway = networkManagementClient.applicationGateways.get(resourceGroup, gatewayName, null, _);
      } catch (e) {
        if (e.statusCode === 404) {
          applicationGateway = null;
        } else {
          throw e;
        }
      } finally {
        progress.end();
      }

      if(!applicationGateway) {
        throw new Error(util.format($('application gateways with name "%s" not found in the resource group "%s"'), gatewayName, resourceGroup));
      }

      var items = applicationGateway.redirectConfigurations;
      cli.interaction.formatOutput(items, function(items) {
        if (items.length === 0) {
          cli.output.warn($('No redirect configurations found'));
        } else {
          cli.output.table(items, function (row, item) {
            row.cell($('Name'), item.name);
            var resInfo = resourceUtils.getResourceInformation(item.id);
            row.cell($('Resource group'), resInfo.resourceGroup);
            row.cell($('Provisioning state'), item.provisioningState);
          });
        }
      });
    });
};
