/**
 * Copyright (c) Microsoft.  All rights reserved.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// Warning: This code was generated by a tool.
// 
// Changes to this file may cause incorrect behavior and will be lost if the
// code is regenerated.

/*

Generated Command List:

azure deployment abort-migration 
--service-name $p0
--name $p1

azure deployment commit-migration 
--service-name $p0
--name $p1

azure deployment get-by-name 
--service-name $p0
--name $p1

azure deployment get-by-slot-parameters deployment-slot delete
--parameter-file $f

azure deployment get-by-slot 
--service-name $p0
--deployment-slot $p1

azure deployment prepare-migration-parameters prepare-deployment-migration-parameters set
--parameter-file $f
--parse
--destination-virtual-network $destinationVirtualNetwork
--resource-group-name $resourceGroupName
--subnet-name $subNetName
--virtual-network-name $virtualNetworkName

azure deployment prepare-migration-parameters prepare-deployment-migration-parameters delete
--parameter-file $f
--destination-virtual-network
--resource-group-name
--subnet-name
--virtual-network-name

azure deployment prepare-migration 
--service-name $p0
--name $p1
--destination-virtual-network $p20
--resource-group-name $p21
--subnet-name $p22
--virtual-network-name $p23

azure deployment validate-migration-parameters prepare-deployment-migration-parameters set
--parameter-file $f
--parse
--destination-virtual-network $destinationVirtualNetwork
--resource-group-name $resourceGroupName
--subnet-name $subNetName
--virtual-network-name $virtualNetworkName

azure deployment validate-migration-parameters prepare-deployment-migration-parameters delete
--parameter-file $f
--destination-virtual-network
--resource-group-name
--subnet-name
--virtual-network-name

azure deployment validate-migration 
--service-name $p0
--name $p1
--destination-virtual-network $p20
--resource-group-name $p21
--subnet-name $p22
--virtual-network-name $p23


*/

'use strict';var fs = require('fs');

var profile = require('../../../util/profile');
var utils = require('../../../util/utils');

var $ = utils.getLocaleString;exports.init = function (cli) {

/*
  Deployment AbortMigration
  --service-name
  --name
*/
  var deploymentAbortMigration = cli.category('service').description('Commands to invoke service management operations.').category('deployment')
  .description($('Commands to manage your deployment.  '));
  deploymentAbortMigration.command('abort-migration [service-name] [name]')
  .description($('The Abort Deployment Operation validates and aborts your deployment for IaaS Classic to ARM migration.'))
  .usage('[options] <service-name> <name>')
  .option('--service-name <service-name>', $('service-name'))
  .option('-n, --name <name>', $('name'))
  .option('-s, --subscription <subscription>', $('The subscription identifier'))
  .execute(function(serviceName, name, options, _) {
    if (!serviceName) {
      serviceName = cli.interaction.promptIfNotGiven($('service-name : '), serviceName, _);
    }

    cli.output.verbose('serviceName = ' + serviceName);
    if (!name) {
      name = cli.interaction.promptIfNotGiven($('name : '), name, _);
    }

    cli.output.verbose('name = ' + name);
    var subscription = profile.current.getSubscription(options.subscription);
    var computeManagementClient = utils.createComputeClient(subscription);
    var result = computeManagementClient.deployments.abortMigration(serviceName, name, _);
    if (result) {
      cli.output.json(result);
    }
  });
/*
  Deployment CommitMigration
  --service-name
  --name
*/
  var deploymentCommitMigration = cli.category('service').description('Commands to invoke service management operations.').category('deployment')
  .description($('Commands to manage your deployment.  '));
  deploymentCommitMigration.command('commit-migration [service-name] [name]')
  .description($('The Commit Deployment Operation validates and commits your deployment for IaaS Classic to ARM migration.'))
  .usage('[options] <service-name> <name>')
  .option('--service-name <service-name>', $('service-name'))
  .option('-n, --name <name>', $('name'))
  .option('-s, --subscription <subscription>', $('The subscription identifier'))
  .execute(function(serviceName, name, options, _) {
    if (!serviceName) {
      serviceName = cli.interaction.promptIfNotGiven($('service-name : '), serviceName, _);
    }

    cli.output.verbose('serviceName = ' + serviceName);
    if (!name) {
      name = cli.interaction.promptIfNotGiven($('name : '), name, _);
    }

    cli.output.verbose('name = ' + name);
    var subscription = profile.current.getSubscription(options.subscription);
    var computeManagementClient = utils.createComputeClient(subscription);
    var result = computeManagementClient.deployments.commitMigration(serviceName, name, _);
    if (result) {
      cli.output.json(result);
    }
  });
/*
  Deployment GetByName
  --service-name
  --name
*/
  var deploymentGetByName = cli.category('service').description('Commands to invoke service management operations.').category('deployment')
  .description($('Commands to manage your deployment.  '));
  deploymentGetByName.command('get-by-name [service-name] [name]')
  .description($('The Get Deployment By Name operation returns configuration information, status, and system properties for a deployment.  (see http://msdn.microsoft.com/en-us/library/windowsazure/ee460804.aspx for more information)'))
  .usage('[options] <service-name> <name>')
  .option('--service-name <service-name>', $('service-name'))
  .option('-n, --name <name>', $('name'))
  .option('-s, --subscription <subscription>', $('The subscription identifier'))
  .execute(function(serviceName, name, options, _) {
    if (!serviceName) {
      serviceName = cli.interaction.promptIfNotGiven($('service-name : '), serviceName, _);
    }

    cli.output.verbose('serviceName = ' + serviceName);
    if (!name) {
      name = cli.interaction.promptIfNotGiven($('name : '), name, _);
    }

    cli.output.verbose('name = ' + name);
    var subscription = profile.current.getSubscription(options.subscription);
    var computeManagementClient = utils.createComputeClient(subscription);
    var result = computeManagementClient.deployments.getByName(serviceName, name, _);
    if (result) {
      cli.output.json(result);
    }
  });
/*
  Deployment GetBySlot
  --service-name
  --deployment-slot
=============================================
0
*/
  var deploymentGetBySlot = cli.category('service').description('Commands to invoke service management operations.').category('deployment')
  .description($('Commands to manage your deployment.  '));
  deploymentGetBySlot.command('get-by-slot [service-name] [deployment-slot]')
  .description($('The Get Deployment By Slot operation returns configuration information, status, and system properties for a deployment.  (see http://msdn.microsoft.com/en-us/library/windowsazure/ee460804.aspx for more information)'))
  .usage('[options] <service-name> <deployment-slot>')
  .option('--service-name <service-name>', $('service-name'))
  .option('--deployment-slot <deployment-slot>', $('deployment-slot'))
  .option('-s, --subscription <subscription>', $('The subscription identifier'))
  .execute(function(serviceName, deploymentSlot, options, _) {
    if (!serviceName) {
      serviceName = cli.interaction.promptIfNotGiven($('service-name : '), serviceName, _);
    }

    cli.output.verbose('serviceName = ' + serviceName);
    if (!deploymentSlot) {
      deploymentSlot = cli.interaction.promptIfNotGiven($('deployment-slot : '), deploymentSlot, _);
    }

    cli.output.verbose('deploymentSlot = ' + deploymentSlot);
    var subscription = profile.current.getSubscription(options.subscription);
    var computeManagementClient = utils.createComputeClient(subscription);
    var result = computeManagementClient.deployments.getBySlot(serviceName, deploymentSlot, _);
    if (result) {
      cli.output.json(result);
    }
  });
/*
  Deployment PrepareMigration
  --service-name
  --name
  --parameters
=============================================
{
  "destinationVirtualNetwork":"",
  "resourceGroupName":"",
  "subNetName":"",
  "virtualNetworkName":""
}
*/
  var deploymentPrepareMigration = cli.category('service').description('Commands to invoke service management operations.').category('deployment')
  .description($('Commands to manage your deployment.  '));
  deploymentPrepareMigration.command('prepare-migration [service-name] [name] [destination-virtual-network] [resource-group-name] [subnet-name] [virtual-network-name]')
  .description($('The Prepare Deployment Operation validates and prepares your deployment for IaaS Classic to ARM migration.'))
  .usage('[options] <service-name> <name> <destination-virtual-network> <resource-group-name> <subnet-name> <virtual-network-name>')
  .option('--service-name <service-name>', $('service-name'))
  .option('-n, --name <name>', $('name'))
  .option('--destination-virtual-network <destination-virtual-network>', $('destination-virtual-network'))
  .option('-g, --resource-group-name <resource-group-name>', $('resource-group-name'))
  .option('--subnet-name <subnet-name>', $('subnet-name'))
  .option('--virtual-network-name <virtual-network-name>', $('virtual-network-name'))
  .option('-s, --subscription <subscription>', $('The subscription identifier'))
  .execute(function(serviceName, name, destinationVirtualNetwork, resourceGroupName, subNetName, virtualNetworkName, options, _) {
    if (!serviceName) {
      serviceName = cli.interaction.promptIfNotGiven($('service-name : '), serviceName, _);
    }

    cli.output.verbose('serviceName = ' + serviceName);
    if (!name) {
      name = cli.interaction.promptIfNotGiven($('name : '), name, _);
    }

    cli.output.verbose('name = ' + name);
    var parametersObj = null;
    if (options.parameterFile) {
      cli.output.verbose('Reading file content from: \"' + options.parameterFile + '\"');
      var parametersFileContent = fs.readFileSync(options.parameterFile, 'utf8');
      parametersObj = JSON.parse(parametersFileContent);
    }
    else {
      parametersObj = {};
      cli.output.verbose('destinationVirtualNetwork = ' + destinationVirtualNetwork);
      parametersObj.destinationVirtualNetwork = destinationVirtualNetwork;
      cli.output.verbose('resourceGroupName = ' + resourceGroupName);
      parametersObj.resourceGroupName = resourceGroupName;
      cli.output.verbose('subNetName = ' + subNetName);
      parametersObj.subNetName = subNetName;
      cli.output.verbose('virtualNetworkName = ' + virtualNetworkName);
      parametersObj.virtualNetworkName = virtualNetworkName;
    }
    cli.output.verbose('parametersObj = ' + JSON.stringify(parametersObj));
    var subscription = profile.current.getSubscription(options.subscription);
    var computeManagementClient = utils.createComputeClient(subscription);
    var result = computeManagementClient.deployments.prepareMigration(serviceName, name, parametersObj, _);
    if (result) {
      cli.output.json(result);
    }
  });
/*
  Deployment ValidateMigration
  --service-name
  --name
  --parameters
=============================================
{
  "destinationVirtualNetwork":"",
  "resourceGroupName":"",
  "subNetName":"",
  "virtualNetworkName":""
}
*/
  var deploymentValidateMigration = cli.category('service').description('Commands to invoke service management operations.').category('deployment')
  .description($('Commands to manage your deployment.  '));
  deploymentValidateMigration.command('validate-migration [service-name] [name] [destination-virtual-network] [resource-group-name] [subnet-name] [virtual-network-name]')
  .description($('The Validate Deployment Operation validates your deployment for IaaS Classic to ARM migration.'))
  .usage('[options] <service-name> <name> <destination-virtual-network> <resource-group-name> <subnet-name> <virtual-network-name>')
  .option('--service-name <service-name>', $('service-name'))
  .option('-n, --name <name>', $('name'))
  .option('--destination-virtual-network <destination-virtual-network>', $('destination-virtual-network'))
  .option('-g, --resource-group-name <resource-group-name>', $('resource-group-name'))
  .option('--subnet-name <subnet-name>', $('subnet-name'))
  .option('--virtual-network-name <virtual-network-name>', $('virtual-network-name'))
  .option('-s, --subscription <subscription>', $('The subscription identifier'))
  .execute(function(serviceName, name, destinationVirtualNetwork, resourceGroupName, subNetName, virtualNetworkName, options, _) {
    if (!serviceName) {
      serviceName = cli.interaction.promptIfNotGiven($('service-name : '), serviceName, _);
    }

    cli.output.verbose('serviceName = ' + serviceName);
    if (!name) {
      name = cli.interaction.promptIfNotGiven($('name : '), name, _);
    }

    cli.output.verbose('name = ' + name);
    var parametersObj = null;
    if (options.parameterFile) {
      cli.output.verbose('Reading file content from: \"' + options.parameterFile + '\"');
      var parametersFileContent = fs.readFileSync(options.parameterFile, 'utf8');
      parametersObj = JSON.parse(parametersFileContent);
    }
    else {
      parametersObj = {};
      cli.output.verbose('destinationVirtualNetwork = ' + destinationVirtualNetwork);
      parametersObj.destinationVirtualNetwork = destinationVirtualNetwork;
      cli.output.verbose('resourceGroupName = ' + resourceGroupName);
      parametersObj.resourceGroupName = resourceGroupName;
      cli.output.verbose('subNetName = ' + subNetName);
      parametersObj.subNetName = subNetName;
      cli.output.verbose('virtualNetworkName = ' + virtualNetworkName);
      parametersObj.virtualNetworkName = virtualNetworkName;
    }
    cli.output.verbose('parametersObj = ' + JSON.stringify(parametersObj));
    var subscription = profile.current.getSubscription(options.subscription);
    var computeManagementClient = utils.createComputeClient(subscription);
    var result = computeManagementClient.deployments.validateMigration(serviceName, name, parametersObj, _);
    if (result) {
      cli.output.json(result);
    }
  });


};
